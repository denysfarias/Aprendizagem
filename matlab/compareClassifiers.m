function [h, pValue] = compareClassifiers(globalErrorPerRoundMatrix)
%COMPARECLASSIFIERS Classifiers comparison through t-student test.
%
% INPUT:
% globalErrorPerRoundMatrix (r,c): global error
% values from c classifiers for r rounds.
%
% OUTPUT:
% h (,):
% pValue (,):
%
% {dlf2,dvro}@cin.ufpe.br

% TODO